## 1. Setup

- [x] 1.1 Create the `runtime/tests/adversarial/fixtures/` directory structure and verify it exists

## 2. Adversarial Test Fixtures

- [x] 2.1 Create `runtime/tests/adversarial/fixtures/adversarial_path.json` containing PATH category fixtures from PATH-001 through PATH-010, and verify JSON is well-formed
- [x] 2.2 Create `runtime/tests/adversarial/fixtures/adversarial_reg.json` containing REG category fixtures, and verify JSON is well-formed
- [x] 2.3 Create `runtime/tests/adversarial/fixtures/adversarial_snap.json` containing SNAP category fixtures, and verify JSON is well-formed
- [x] 2.4 Create `runtime/tests/adversarial/fixtures/adversarial_content.json` containing CONTENT category fixtures, and verify JSON is well-formed
- [x] 2.5 Create `runtime/tests/adversarial/fixtures/adversarial_cap.json` containing CAP category fixtures, and verify JSON is well-formed
- [x] 2.6 Create `runtime/tests/adversarial/fixtures/adversarial_disc.json` containing DISC category fixtures, and verify JSON is well-formed
- [x] 2.7 Create `runtime/tests/adversarial/fixtures/adversarial_native.json` containing NATIVE category fixtures up to NATIVE-010, and verify JSON is well-formed

## 3. Redaction Test Fixtures

- [x] 3.1 Create `runtime/tests/adversarial/fixtures/redaction.json` containing secret redaction and truncation bounds cases (e.g., token replacement, over-bound truncation) as defined in `data-classification.md`, and verify JSON is well-formed
