> **Apply contract:** Completing this checklist creates and validates documentation under `blueprint/` only. It MUST NOT create application/test/migration/infrastructure source, install runtime dependencies, provision services, use provider credentials, invoke a model, or execute contest data. Every task must leave reviewable file-based evidence.

## 1. Blueprint Foundation

- [x] 1.1 [TV1/TV6, Gate: foundation] Create `blueprint/README.md`, the planned directory structure, and `blueprint/manifest.yaml`; state purpose, documentation-only scope, authority hierarchy, version, status vocabulary, owners, and the rule that blueprint completion is not implementation completion.
- [x] 1.2 [TV1/TV5, Gate: foundation] Define manifest fields for artifact path, role, normative status, version, TV owner, governing requirements, approval state, content digest, and dependencies; include valid and deliberately invalid examples for review.
- [x] 1.3 [TV1–TV6, Gate: foundation] Create a canonical vocabulary and identifier registry aligned with `docs/01-glossary.md` for runs, states, entities, events, schemas, flags, errors, timestamps, money, tokens, digests, and versions.
- [x] 1.4 [TV1/TV6, Gate: foundation] Create `delivery/requirement-traceability.md` with one row for every OpenSpec requirement and Given/When/Then scenario, leaving no generic document-level mappings.
- [x] 1.5 [TV1–TV6, Gate: foundation] Define the ADR template and decision registry with `Proposed|Accepted|Superseded|Rejected` status, alternatives, evidence, consequences, owner, approval requirement, and affected work packages.
- [x] 1.6 [TV1/TV6, Gate: foundation] Record a scope audit proving the blueprint tree contains documentation, schemas, examples, and diagrams only and identifying patterns that would constitute prohibited implementation output.

## 2. Architecture and Decision Records

- [x] 2.1 [TV1/TV6, Gate: architecture] Create `ADR-001-technology-stack.md` comparing viable language, backend, frontend, relational database, migration, packaging, and worker/queue options against team proficiency, provider support, async maturity, schema tooling, reproducibility, and operational simplicity; use `Proposed` status when approval evidence is unavailable.
- [x] 2.2 [TV1/TV5, Gate: architecture] Create `ADR-002-provider-contract.md` comparing direct SDK and gateway approaches, defining the one-real-plus-two-deterministic profile strategy, fidelity criteria, credential boundary, and the approval needed before future adapter implementation.
- [x] 2.3 [TV5, Gate: architecture] Create `ADR-003-baseline-protocol.md` documenting arm fairness, intentional differences, SourceBundle choice, repeat/scoring rules, cost/run calculation and pricing-version policy, and the approval needed before any paid experiment.
- [x] 2.4 [TV1/TV4/TV5/TV6, Gate: architecture] Create the system-context diagram and companion invariant table for users, providers, source registry, scorer-only ground truth, evaluation control plane, and exported results.
- [x] 2.5 [TV1/TV4/TV6, Gate: architecture] Create the container and trust-boundary view for desktop client, local daemon, datastore/queue, worker, provider, ephemeral workspace, evaluator, scorer, and ground-truth store; explicitly show forbidden data-flow edges.
- [x] 2.6 [TV1–TV6, Gate: architecture] Create the component and ownership view for runtime capabilities, their public/domain/application/ports/adapters boundaries, desktop modules, technical platform, composition roots and allowed/forbidden dependencies.
- [x] 2.7 [TV1/TV6, Gate: architecture] Create the normative Judge lifecycle diagram and transition table covering accepted, queued, running, completed, failed, cancelled, and budget-exhausted states, transition authority, terminal immutability, and stale-worker behavior.
- [x] 2.8 [TV1/TV2/TV3/TV4/TV6, Gate: architecture] Create sequence diagrams and invariant tables for submission, queue claim, context preflight, provider/tool iteration, structured repair, successful completion, cancellation, budget exhaustion, permanent/transient provider failure, tool denial, and redelivery.
- [x] 2.9 [TV1–TV6, Gate: architecture] Create `delivery/extension-roadmap.md` locating future seams for additional real providers, cross-provider evaluation, static-analyzer/general-agent baselines, PoC verification, Audit mode, long-term memory, compaction, advanced tools, and offline replay without designing those implementations.

## 3. Domain and Boundary Contracts

- [x] 3.1 [TV1/TV5/TV6, Gate: contracts] Create `contracts/domain-model.md` defining canonical commands, states, events, terminal outcomes, `CandidateFinding`, `SourceSnapshot`, scorer-only `GroundTruthLabel`, context allocation, provider attempt, tool call, verdict, evidence, and experiment-cell semantics.
- [x] 3.2 [TV1/TV5, Gate: contracts] Define canonical serialization and digest rules for candidate findings, source snapshots, resolved configuration, prompt/tool/schema contents, and experiment identity with boundary and failure examples.
- [x] 3.3 [TV1/TV5, Gate: contracts] Create `contracts/provider-contract.md` with normalized request/response, message, structured-output, tool-call, capability, usage, identity, request metadata, latency, pricing, transient/permanent error, retry, and credential-exclusion semantics.
- [x] 3.4 [TV1/TV5, Gate: contracts] Define one real-provider and two deterministic conformance profiles and a shared conformance-case matrix without invoking a provider or selecting credentials.
- [x] 3.5 [TV3/TV4, Gate: contracts] Create `contracts/tool-contracts.md` for versioned `read_file`, `list_dir`, `glob`, and text search definitions, limits, deterministic ordering, truncation, actionable errors, registry dispatch, and exclusion of generic shell/network operations.
- [x] 3.6 [TV2/TV3/TV5, Gate: contracts] Create `contracts/context-and-budget.md` covering context-limit provenance, mandatory output reserve, allocation buckets, token estimates, immutable inputs, deterministic tool-result transformation, stop-condition precedence, and telemetry.
- [x] 3.7 [TV1/TV4/TV5, Gate: contracts] Create `contracts/judge-verdict.schema.json`, its normative semantic table, and valid/invalid fixtures for severity, confidence, mandatory resolvable evidence, label-normalization version, and fixed `verification_status: unverified`.
- [x] 3.8 [TV1/TV5/TV6, Gate: contracts] Create `contracts/trajectory-events.schema.json` and examples for lifecycle, model step, provider attempt, context allocation, tool call, transformation, security, cancellation, terminal, and scoring-reference events.
- [x] 3.9 [TV6/TV1/TV5, Gate: contracts] Create `contracts/async-api.openapi.yaml` with reusable schemas and complete examples for idempotent Judge submission, run retrieval, cursor-paginated events, cancellation, validation/conflict/not-found errors, partial states, and a non-public access-controlled local-runtime binding.

## 4. Persistence Blueprint

- [x] 4.1 [TV6/TV1/TV5, Gate: persistence] Create `persistence/erd.md` covering source snapshot, candidate finding, run, immutable run configuration, step, provider attempt, tool call, verdict, evidence, security event, experiment, and experiment cell with cardinalities and ownership.
- [x] 4.2 [TV6/TV4/TV5, Gate: persistence] Create `persistence/field-dictionary.md` with type, nullability, constraint, index, sensitivity, retention, source, digest semantics, and API/export exposure for every planned field.
- [x] 4.3 [TV6/TV1, Gate: persistence] Create `persistence/consistency-and-idempotency.md` defining append-only event ordering, `(run_id, sequence)` uniqueness, compare-and-set transitions, atomic terminal commits, idempotency-key records, job claim/redelivery, cancellation, and partial-worker-failure behavior.
- [x] 4.4 [TV6/TV4/TV5, Gate: persistence] Define the complete reproduction snapshot, content-addressed reference rules, deterministic pre-persistence redaction, exact sanitized model-visible storage, mismatch reporting, and the limit on claims of stochastic replay.

## 5. Security Blueprint

- [x] 5.1 [TV4/TV5, Gate: security] Create `security/data-classification.md` classifying candidate, source, ground truth, adjudication, scoring metadata, manifests, provider credentials, prompts, tool results, trajectories, exports, and safe security events across every component.
- [x] 5.2 [TV4/TV3, Gate: security] Create `security/workspace-policy.md` defining opaque snapshot lookup, immutable digest preflight, ephemeral read-only `source/`, canonicalization before I/O, root containment, post-symlink checks, no network, no generic shell, bounded reads, and teardown.
- [x] 5.3 [TV4/TV1/TV6, Gate: security] Create `security/threat-model.md` with assets, actors, entry points, trust boundaries, misuse cases, mitigations, residual risks, and explicit claims the MVP does and does not make.
- [x] 5.4 [TV4/TV3/TV6, Gate: security] Create `security/adversarial-acceptance-catalog.md` with stable test IDs and expected safe outcomes for absolute paths, traversal encodings, symlink escapes, aliases, adjacent labels/reports, tampered snapshots, missing/oversized files, prompt injection, secret fixtures, blocked arguments, and network attempts.
- [x] 5.5 [TV4/TV5/TV6, Gate: security] Audit the planned data flows to prove ground truth cannot enter request construction, workspaces, tools, provider calls, ordinary logs, trajectories, local API responses, desktop views, or exports and that redaction occurs before persistence.

## 6. Evaluation Blueprint

- [x] 6.1 [TV5/TV1/TV2/TV3, Gate: evaluation] Create `evaluation/flags-and-ablation.yaml` mapping every optional result-affecting behavior to type, default, dependencies, arm values, telemetry, run-snapshot field, enabled/disabled acceptance IDs, and version/digest rules; list non-disableable safety invariants separately.
- [x] 6.2 [TV5/TV1/TV2, Gate: evaluation] Create `evaluation/baseline-protocol.md` defining shared direct/harness fields, intentional differences, exact prompt references, token/output fairness, one-call direct semantics, harness flags, drift rejection, and protocol versioning.
- [x] 6.3 [TV5/TV2/TV4, Gate: evaluation] Create `evaluation/source-bundle-v1.md` defining referenced-file priority, allowlisted file classes, normalized lexical ordering, stable delimiters, input-budget filling, omission markers, digest rules, and independently reproducible examples.
- [x] 6.4 [TV5/TV4, Gate: evaluation] Create `evaluation/contest-manifest.schema.json` plus valid, split-leaking, tampered, and missing-cutoff examples covering provenance, publication time, immutable revision/digest, label normalization, whole-contest assignment, freeze state, and contamination buckets.
- [x] 6.5 [TV5/TV4/TV6, Gate: evaluation] Document the scorer-only store and post-terminal join sequence, including credential separation, clean test execution, case identifiers, scoring outputs, and forbidden paths back into agent-visible records.
- [x] 6.6 [TV5/TV6, Gate: evaluation] Create `evaluation/scoring-and-reporting.md` defining experiment-cell identity, declared repeats/order, resume/retry rules, positive class, terminal-failure denominators, precision, recall, completion/schema-failure rate, cost, latency, agreement/dispersion, baseline delta, contamination buckets, and JSON/CSV fields.
- [x] 6.7 [TV5/TV1/TV6, Gate: evaluation] Add paired direct/harness example packets that use the same canonical case and fairness fields, show all declared arm differences, contain no real ground truth in agent-visible material, and link to their expected scoring record separately.

## 7. Desktop Trace Experience and Implementation Handoff

- [x] 7.1 [TV6, Gate: experience] Create the information architecture for desktop runtime connection, Judge submission, run view, ordered trace, verdict/evidence review, configuration summary, usage/cost summary, and safe error/security details; task 9.3 moves the baseline artifact from `ui/` to `desktop/`.
- [x] 7.2 [TV6/TV5, Gate: experience] Create the submission and asynchronous-status wireframes with canonical finding fields, registered snapshot selection, provider/model/config choices, idempotency, validation feedback, `run_id` navigation, and polling behavior.
- [x] 7.3 [TV6/TV1/TV2/TV3/TV4, Gate: experience] Create the trace-view wireframe and component-state matrix for model steps, provider attempts, tool calls, context allocation, transformations, errors, tokens, latency, cost, and security blocks.
- [x] 7.4 [TV6/TV1/TV4, Gate: experience] Create completed, failed, cancelled, and budget-exhausted terminal-state wireframes; show final verdict only for completed runs and make `unverified` prominent with authorized evidence navigation.
- [x] 7.5 [TV6/TV4, Gate: experience] Perform a UI disclosure review ensuring no wireframe or example exposes ground truth, secrets, raw host paths, prohibited originals, or unbounded blocked arguments.
- [x] 7.6 [TV1–TV6, Gate: handoff] Create `delivery/dependency-and-ownership.md` mapping modules, ports, artifacts, accountable TV owner, collaborators, allowed dependencies, review responsibility, and pre-implementation ADR gates.
- [x] 7.7 [TV1–TV6, Gate: handoff] Create `delivery/implementation-work-packages.md` that decomposes future code work into dependency-ordered packages with contract inputs and acceptance evidence, while containing no source-level implementation or schedule claims.

## 8. Blueprint Validation and Acceptance

- [x] 8.1 [TV1/TV5/TV6, Gate: validation] Validate all JSON Schema files against their valid and invalid examples; record validator/version, commands, expected failures, and results in `delivery/validation-report.md` without generating runtime classes.
- [x] 8.2 [TV6/TV1, Gate: validation] Validate the OpenAPI document and trace one success plus every defined error/cancellation path through reusable schemas and examples; record evidence.
- [x] 8.3 [TV5/TV6, Gate: validation] Validate YAML syntax, canonicalization assumptions, manifest completeness, unique identifiers, dependency references, and reproducible content digests; record evidence.
- [x] 8.4 [TV1–TV6, Gate: validation] Check every internal link, heading anchor, diagram companion table, schema reference, glossary term, enum, field name, state, event type, flag, and version identifier for cross-artifact consistency.
- [x] 8.5 [TV4/TV5, Gate: validation] Run the blueprint-only ground-truth isolation audit and ablation-readiness audit; fail acceptance for any unclassified data flow or optional result-affecting behavior missing flag, telemetry, snapshot, and enabled/disabled acceptance mappings.
- [x] 8.6 [TV1–TV6, Gate: validation] Complete bidirectional traceability so every OpenSpec requirement/scenario points to exact blueprint evidence and every normative blueprint section points back to governing requirements and a future work package.
- [x] 8.7 [TV1–TV6, Gate: validation] Review all ADR statuses and list unresolved approval gates explicitly; confirm that none is silently treated as accepted and none prevents the documentation-only blueprint from being complete.
- [x] 8.8 [TV1/TV6, Gate: acceptance] Run `openspec validate bootstrap-judge-mode-mvp --strict`, finalize `delivery/validation-report.md` with the statement `Blueprint only — no implementation evidence`, inventory all produced files/digests, and confirm that no executable implementation artifact was created.

## 9. Architecture Rebaseline and Hardening

- [x] 9.1 [TV1/TV6, Timeline: C01 rebaseline, Gate: architecture] Update `ADR-001` to `Accepted` at stack-family scope and create accepted `ADR-004` for clean local OpenCode snapshot `14f0bf64a19493110b51f5fdeb9c1c1bba5dd3f5`. Record a row-level provenance matrix containing snapshot-relative evidence path, observed pattern, adopt/adapt/reject disposition, Judge rationale, destination blueprint artifact, forbidden carry-over and future validation; record MIT provenance without authorizing reuse; prohibit forks, submodules/subtrees, workspace/package dependencies, generated/vendor/copied source and automatic upstream sync; define versioned newer-snapshot review plus superseding-ADR rules. Verification evidence: clean snapshot identity, artifact/package/lock/submodule inventory, zero copied/imported OpenCode source or dependencies, resolved provenance destinations, and explicit notice that future source/package reuse requires a separate license/security/dependency review.
- [x] 9.2 [TV1–TV6, Timeline: C01 rebaseline, Gate: source-architecture] Create accepted `ADR-005-capability-first-modular-monolith.md`, `contracts/registry.yaml`, and replace the old layer-first/backend/web layout in `architecture/physical-repository-layout.md` and `architecture/components-and-ownership.md` with the exact target tree: canonical `contracts/`; Python `runtime/`; `apps/desktop/`; `config/`; `datasets/`; `packaging/`; `docs/`; `blueprint/`; and `openspec/`. Inside the runtime define `shared_kernel`, capability modules `run_control`, `model_gateway`, `source_access`, `agent_runtime`, `judge`, `evaluation`, `scoring`, technical-only `platform`, composition-only `entrypoints/{daemon,worker,evaluator,scorer}`, generated contracts, module-owned migrations and architecture/module/contract/integration/adversarial/e2e tests. For each capability record `public/domain/application/ports/adapters/resources` ownership while prohibiting empty ceremonial folders, global business adapters, shared-kernel business logic, cross-module internal imports, cross-module table queries and independent service releases. Publish an allowed dependency matrix, module/public API catalog, table/migration owner matrix, scorer-only import-deny matrix, contract generation/exposure allowlists, TV1–TV6 reviewer map, Audit/VerificationRunner seams, and future architecture-test rules that allow cross-capability imports only through `harness.modules.<capability>.public`.
- [x] 9.3 [TV1/TV4/TV6, Timeline: C01→C02, Gate: product-topology] Create accepted `ADR-006-desktop-local-runtime.md`, proposed `ADR-007-desktop-shell.md`, `architecture/desktop-runtime-topology.md`, and `desktop/local-runtime-connection.md`; rename/reconcile the prior `ui/` blueprint section as `desktop/`. Specify a downloadable React/TypeScript/Vite renderer plus native shell over a local headless runtime, with daemon/worker/evaluator/scorer and PostgreSQL remaining authoritative and independent of window lifecycle. Define loopback or OS-IPC-only binding, protected installation-scoped endpoint discovery and access control, health/version/contract-digest/capability handshake, fail-closed incompatibility, explicit shutdown/update/cancel operations, reconnect/cursor recovery, coordinated release compatibility and local-runtime/desktop packaging boundaries. Keep public exposure, remote multi-tenancy and a database-free single-file installer out of MVP. ADR-007 MUST compare Tauri and Electron, and any qualifying Python-hosted option, against OS support, process lifecycle, credential store, repository picker, signing/updater maturity, bundle/reproducibility, crash recovery and team tooling; leave it `Proposed` and explicitly block native-shell/installer/updater source until accepted without blocking blueprint or runtime work.
- [x] 9.4 [TV1/TV2/TV3/TV5, Timeline: C01→C02, Gate: runtime/provider] Update `ADR-002` to `Accepted` at integration-strategy scope; create `architecture/agent-runtime-boundaries.md`, `providers/provider-profile.schema.json`, proposed `providers/real-primary.profile.yaml`, and `providers/deterministic-profiles.md`; reconcile provider contracts, lifecycle, sequences and conformance mapping around the `model_gateway.public` project port, OpenAI Responses API through the official asynchronous Python SDK as the first adapter under `modules/model_gateway/adapters/outbound/providers/openai`, explicit committed history, non-streaming one-attempt calls, disabled SDK retry/automatic tool execution, local custom functions only, independent verdict validation, and rejection of LiteLLM, external proxies, Agents SDK orchestration, hosted tools and provider-owned loops in MVP. Keep provider SDKs and credentials absent from desktop and other module internals. Keep exact model snapshot, SDK version, capability/cutoff evidence, pricing, credential owner, paid-call ceiling and approval as pre-network fields of versioned `real-primary`; ensure direct and harness arms share the same accepted profile and deterministic profiles remain usable without it.
- [x] 9.5 [TV5/TV1/TV2, Timeline: C01→C03, Gate: evaluation-method] Update `ADR-003` to `Accepted` at methodology scope and create `evaluation/experiment-profile.schema.json`, proposed `evaluation/rq1-confirmatory-v1.profile.yaml`, and content-addressed `evaluation/prompts/{judge-core,direct-wrapper,harness-wrapper}.md`; record matched case/repeat pairs, equal total logical-token accounting including cached/repeated context, wall-clock separation, shared Judge semantics, intentional wrapper differences, primary no-retry symmetry, deterministic pair scheduling, drift rejection, and the rule that changing experiment values version the profile rather than the ADR. Verification evidence: schema-valid complete/incomplete profiles, prompt digests, paired budget examples, and a pre-network rejection example.
- [x] 9.6 [TV5/TV4/TV6, Timeline: C02→C03, Gate: evaluation-validity] Reconcile baseline protocol, SourceBundle, contest manifest/schema/examples, flags/ablation, scoring/reporting, paired packets and scorer flow around train/validation/frozen-test separation, whole-contest plus source-family grouping, post-cutoff buckets, contest-cluster confidence intervals, repeat-as-variability semantics, and predeclared precision-gain/recall-loss/completion gates; keep retry-enabled execution under a distinct flag, telemetry field, immutable snapshot value, enabled/disabled acceptance IDs and experiment identity. Verification evidence: split-leak rejection, retry asymmetry rejection, pseudo-replication review, threshold-before-test review, and `mixed` classification when recall or completion fails.
- [x] 9.7 [TV3/TV4/TV6, Timeline: C01→C02, Gate: security/source] Reconcile tool, workspace, local registration, threat-model, data-flow, desktop and adversarial artifacts so the native repository picker supplies only short-lived control-plane input to `source_access`; runtime canonicalization, authorization, immutable snapshot/digest and opaque `source_snapshot_id` precede every run; raw host paths are absent from provider traffic, trajectories, ordinary logs, run resources and desktop traces. Keep shell, mutation, network, hosted tools, plugin discovery and arbitrary execution structurally absent from Judge runtime, and document a separate future `VerificationRunner` capability/entrypoint boundary instead of expanding Judge authority.
- [x] 9.8 [TV1/TV6/TV4, Timeline: C01→C02, Gate: persistence/local-api/desktop] Reconcile topology, consistency rules, ERD, field dictionary, `contracts/async-api.openapi.yaml`, desktop information architecture/wireframes and delivery artifacts around PostgreSQL outbox/job records, claims/leases/versions, compare-and-set lifecycle authority, polling-first finite cursors, desktop-independent recovery, runtime unavailable/unauthorized/incompatible/reconnecting states and explicit lifecycle operations. Extend the local OpenAPI with access-control security schemes or OS-IPC equivalence notes plus complete health/runtime-info handshake, source-registration, submission, retrieval, event, cancellation and safe lifecycle examples/errors; classify a selected raw path as ephemeral sensitive control-plane input and prohibit it from run/event schemas. Define the later canonical source `contracts/openapi/local-runtime.v1.openapi.yaml`, generated-client destination `apps/desktop/ui/src/generated/runtime-client/`, public-schema allowlist and drift checks; prohibit manual generated edits and direct desktop imports/access to Python, databases, providers, tools, credentials or scorer data. Exclude SQLite, Redis, desktop cache and process memory as MVP authorities.
- [x] 9.9 [TV4/TV5/TV6, Timeline: C01→C03, Gate: scorer-isolation] Reconcile architecture, data classification, ground-truth audit, contracts registry, ERD/table ownership, evaluation and delivery views with the top-level `scoring` capability plus separate scorer entrypoint/process identity, role/schema or equivalent credential boundary, scorer-only generated-schema set, forbidden daemon/worker/evaluator/desktop imports and grants, post-terminal joining, canonical identifier-only inputs and approved non-ground-truth score outputs through a versioned `evaluation.public` acceptance contract. Forbid the reverse evaluation-to-scoring import. Verification evidence MUST prove that no non-scorer composition root can import `scoring` or construct a label adapter/query path and that scorer-only schemas cannot enter the daemon OpenAPI or desktop generator.
- [x] 9.10 [TV1–TV6, Timeline: C01 rebaseline, Gate: validation] Update README, blueprint tree/manifest, decision registry, vocabulary, traceability, ownership, implementation work packages and TV1–TV6 timeline, extension roadmap, scope audit, examples and validation report for tasks 9.1–9.9. Remove stale `backend/`, public-web, trusted-interface, global-adapter and layer-first claims. Validate links; provider/experiment-profile schemas; JSON schemas; OpenAPI/YAML; prompt/artifact digests; every Given/When/Then mapping; desktop/runtime version and lifecycle states; raw-path exclusion; ground-truth isolation; ablation coverage; contest/source-family splits; logical-token accounting; primary retry symmetry; contest-aware inference; predeclared RQ1 gates; OpenCode provenance/no-copy evidence; module/public dependency and table-owner matrices; generated-client/scorer-only allowlist drift plans; ADR-002/ADR-003 versus profile statuses; ADR-005/ADR-006 accepted scope; ADR-007 proposed gate; pre-network rejection of incomplete real profiles; and strict OpenSpec conformance. End with `Blueprint only — no implementation evidence`, inventory every produced documentation file/digest, and confirm no application/test/migration/infrastructure source, dependency installation, service provisioning, credential use, model call or contest execution occurred.

## 10. ADR-007 Tauri Decision Ratification

> Tasks 9.3 and 9.10 record the previously completed `Proposed` baseline. The unchecked tasks below supersede only that ADR-007 status/output. Applying them remains documentation-only and MUST NOT create `src-tauri/`, Rust/TypeScript/Python source, package manifests, installers, signing material, updater infrastructure, services or readiness-test results.

- [x] 10.1 [TV6/TV1/TV4, Timeline: C01→C02 rebaseline, Gate: desktop-shell-decision] Revise `decisions/ADR-007-desktop-shell.md` to `Accepted` version `adr-007-v2` at architecture-choice scope with the project-owner approval recorded. Select Tauri 2 plus a narrow Rust host; retain the full Tauri/Electron/Python-hosted comparison and official-source evidence table; reject Electron as primary and Python-hosted shells for MVP; require a superseding ADR for host-family replacement. State explicitly that acceptance selects architecture but does not claim three-OS packaging readiness. Reconcile ADR-001/ADR-006 cross-references, `decisions/README.md`, README decision summary and manifest status/dependencies without changing the `Proposed` real-provider or experiment-profile gates.

- [x] 10.2 [TV6/TV1/TV4, Timeline: C01→C02, Gate: native-authority-boundary] Reconcile `architecture/physical-repository-layout.md`, `architecture/components-and-ownership.md`, `architecture/containers-and-trust-boundaries.md`, `architecture/desktop-runtime-topology.md`, `desktop/local-runtime-connection.md` and `desktop/information-architecture.md` around the accepted `apps/desktop/src-tauri/` host. Document standard Tauri configuration/lockfiles, per-window `capabilities/`, project `permissions/`, typed `commands/`, runtime-supervision, credential-store, repository-picker, notification and update-coordinator ownership. Preserve `apps/desktop/ui/` as React/Vite presentation using the generated client; deny renderer access to generic filesystem, shell, process, environment, arbitrary URL, raw credential, direct updater, database, provider, Judge tool and scorer authority; require effective-permission graph review and deny-by-default project commands.

- [x] 10.3 [TV1/TV4/TV6, Timeline: C02, Gate: lifecycle-credential-update] Reconcile the local-runtime/API, security and desktop blueprint evidence with the new asynchronous-run-api and trace-view scenarios. Specify a supervised or detached Python/PostgreSQL runtime whose lifetime is not an ordinary Tauri child; OS-protected rendezvous and credential-store adapters with rotation and fail-closed behavior when no approved secure backend exists; picker-to-registration short-lived path custody; and a project update coordinator over signed Tauri artifacts, OS signing, compatibility manifests, active-work preflight, `reject_if_active|quiesce_then_stop`, database compatibility, post-update handshake and rollback. Extend the threat model and adversarial catalog for undeclared native command invocation, insecure credential fallback, close/crash/reopen, incompatible partial update and updater interruption while preserving ground-truth and raw-path exclusions.

- [x] 10.4 [TV1–TV6, Timeline: C02 rebaseline, Gate: traceability-handoff] Update requirement/scenario traceability, normative backreferences, dependency/ownership maps, implementation work packages, TV1–TV6 timeline, scope audit, extension roadmap, vocabulary and every affected desktop example/wireframe. Map the packaged readiness spike to WP-01/WP-10: build on every claimed OS; dummy-runtime discover/start-or-attach; version/contract/capability mismatch; secure credential unavailable/rotation; picker registration; close/reopen with committed work continuing; renderer native-command denial; signed update active-work/failure/rollback states; clean-machine reproducibility; measured startup/memory/bundle evidence. Keep these as future evidence rather than fabricated completion, and remove every stale current-state claim that ADR-007 is still `Proposed` or that native-shell framework selection remains open.

- [x] 10.5 [TV1–TV6, Timeline: C02 rebaseline, Gate: validation] Recompute manifest/artifact digests and update `delivery/validation-report.md`; validate links, YAML/JSON/OpenAPI syntax, decision registry/status consistency, all newly added Given/When/Then mappings, per-window native permission deny rules, generated-client-only Judge data flow, independent runtime/window lifecycle, credential fail-closed behavior, raw-path exclusion, signed coordinated-update/rollback semantics and strict OpenSpec conformance. End with `Blueprint only — no implementation evidence`, inventory the revised documentation files, confirm ADR-007 is accepted while readiness remains unproven, and confirm no application/test/migration/infrastructure source, dependency installation, service provisioning, credential/signing-key creation, model call or contest execution occurred.
