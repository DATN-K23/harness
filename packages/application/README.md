# Application

Các port hướng vào trong cho agent runtime và application use case.

Package này chỉ mô tả dependency mà core cần. SDK model, filesystem, database và
implementation của tool phải nằm sau các interface này.
